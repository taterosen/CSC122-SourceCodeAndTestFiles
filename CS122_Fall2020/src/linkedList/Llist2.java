package linkedList;

import javax.xml.soap.Node;

public class Llist2 
{
	@Override
	public String toString()
	{ 
		StringBuilder s = new StringBuilder("Linked List: ");
		/*Node ptr = head;
		while(ptr != null)
		{
			s.append(ptr.data);
			ptr = ptr.next;
		}*/
		return s.toString();
	}

}
